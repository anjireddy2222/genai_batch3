# fastapi package install

# apis create

# API
    path -> auth/login
    path -> auth/signup
    path -> auth/reset-password

    Method -> POST, GET, DELTE

# setup virtual environment
python -m venv venv
source ./venv/bin/activate

# pip install fastapi

# uvicorn

app.py or main.py or index.py

uvicron app:app --reload

